var React = require('react');

var ContactUs = React.createClass({

    render(){
        return (
            <section id="sitecontact">
                <header>Contact Us</header>
                <article className="contactus-content">
                Some Contact Us copy
                </article>
            </section>
        );
    }
});

module.exports = ContactUs;